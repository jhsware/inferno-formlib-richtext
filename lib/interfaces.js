'use strict'

import { createInterface } from 'component-registry'

export const IRichTextWidget = createInterface({
    name: 'IRichTextWidget'
})

export const IRichTextAction = createInterface({
    name: 'IRichTextAction'
})
